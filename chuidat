-- LocalScript in StarterPlayerScripts

-- 1) Define the target position
local targetPos = Vector3.new(-100, 10000, -100) 

-- 2) Create and configure the Part
local part = Instance.new("Part")
part.Size        = Vector3.new(100, 100, 100)     -- you can change this
part.Anchored    = true                     -- so it doesn't fall
part.Position    = targetPos
part.BrickColor  = BrickColor.new("Bright yellow")
part.Name        = "SpawnPad"
part.Parent      = workspace

-- 3) Wait for the player’s character to load
local Players = game:GetService("Players")
local player  = Players.LocalPlayer

player.CharacterAdded:Connect(function(char)
    char:WaitForChild("HumanoidRootPart")
    -- 4) Teleport the player
    char.HumanoidRootPart.CFrame = CFrame.new(targetPos)
end)

-- If the character is already loaded (e.g. on re-run), teleport immediately:
if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
    player.Character.HumanoidRootPart.CFrame = CFrame.new(targetPos)
end
